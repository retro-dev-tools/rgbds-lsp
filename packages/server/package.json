{
  "name": "@retro-dev/rgbds-language-server",
  "version": "0.2.0",
  "description": "Language server for RGBDS Game Boy assembly — definitions, references, completion, diagnostics, rename",
  "license": "MIT",
  "keywords": ["lsp", "language-server", "rgbds", "gameboy", "assembly", "gbz80"],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "rgbds-language-server": "dist/index.js"
  },
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "prepublishOnly": "npm run build",
    "check": "node dist/check.js",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.12",
    "tree-sitter": "^0.25.0",
    "@retro-dev/tree-sitter-rgbds": "^0.1.4"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
